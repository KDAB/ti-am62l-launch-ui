import { Spinner } from "std-widgets.slint";

export component AppWindow inherits Window {
    width: 1920px;
    height: 1080px;

    Spinner {
        width: 1080px;
        height: 1080px;

        progress: 42%;
        indeterminate: true;
    }
}
