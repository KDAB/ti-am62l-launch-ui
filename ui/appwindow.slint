import { ProgressIndicator } from "std-widgets.slint";

export component AppWindow inherits Window {
    width: 1920px;
    height: 1080px;

    ProgressIndicator {
        width: 200px;
        height: 20px;

        progress: 0%;
        indeterminate: true;
    }
}
