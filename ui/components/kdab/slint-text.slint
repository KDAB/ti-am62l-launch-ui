import { Typography } from "./styling.slint";

export component SlintText inherits Text {
    font-family: Typography.default-font-family;
    color: Typography.default-color;
    font-size: Typography.default-font-size;

}
