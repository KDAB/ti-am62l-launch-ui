import { Progress } from "progress.slint";

export component SystemInfo inherits VerticalLayout {
    in-out property <string> title <=> text.text;
    in-out property <int> percent <=> indicator.value;

    text := Text {}

    indicator := Progress {}
}
